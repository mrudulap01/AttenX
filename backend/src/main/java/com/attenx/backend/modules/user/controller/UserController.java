package com.attenx.backend.modules.user.controller;

import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.bind.annotation.RequestMapping;

/**
 * Controller handling user management endpoints (CRUD operations for Users, Students, Teachers, Admins).
 */
@RestController
@RequestMapping("/api/v1/users")
public class UserController {
    // TODO: Implement endpoints for managing users
}
